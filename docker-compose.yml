services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data

  trading-engine:
    build: .
    command: ["python", "-m", "app.trading"]
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    depends_on:
      - web
